export type ProductType = {
  id: number;
  title: string;
  price: string;
  category: string;
  description: string;
  image: string;
};

export type cartProducts = {
  id: number;
  title: string;
  image: string;
  quantity: number;
  price: string;
};
